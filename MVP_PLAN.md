# Poker Table Companion MVP

## 產品目標

這個 MVP 的核心不是做一個全自動的籌碼辨識器，而是做一個在牌桌旁真的用得起來的輔助工具。

優先解決的問題：

- 玩家想快速知道自己目前有多少碼
- 玩家想快速知道自己目前有多少 BB
- 玩家想在不中斷牌局太久的情況下更新碼量
- 玩家想記錄 buy-in / rebuy / add-on
- 玩家希望拍照可以幫忙，但不要求 100% 全自動

不優先解決的問題：

- 全自動精準估算每一疊籌碼高度
- 即時追蹤整桌所有玩家的碼量
- 用影片連續辨識整場牌局
- 自動讀取每手牌 action

## MVP 核心定位

產品定位為桌邊助手，而不是純 AI 視覺專案。

MVP 採用：

- 照片輔助
- 手動確認
- 快速輸入

而不是：

- 純 YOLO 自動算碼
- 純深度鏡頭自動估高
- 純影片即時追蹤

## 為什麼先不要把主軸放在 YOLO

YOLO 適合做的事情：

- 找到籌碼區域
- 分出不同顏色的大致位置
- 找到有幾堆物件

YOLO 不擅長直接解的事情：

- 單靠俯視圖精準算出一疊有幾片
- 在堆疊互相遮擋時穩定算數量
- 在光線、陰影、反光變化下維持高準確率

所以如果把需求定成「拍一張照就完全自動算出所有碼量」，開發成本會很高，而且很容易不穩。

更適合 MVP 的做法是：

- 系統先幫忙偵測
- 使用者快速修正
- 以少量手動操作換取高可用性

## 建議的拍照規則

為了讓拍照變簡單，系統不要試圖理解所有自然擺放，而是要求使用者配合一些規則。

建議規則：

- 同一顏色盡量堆成一疊或數疊
- 不同顏色分開放
- 拍攝時以自己的籌碼區域為主
- 盡量從上往下拍
- 讓每一疊的頂面看得清楚
- 零散籌碼可以放在旁邊單獨區域

如果使用者願意多配合一點，可以再加上：

- 每個顏色固定放在指定欄位
- 滿一疊固定代表固定顆數，例如 20 顆
- 零頭籌碼平鋪而不是混在堆疊裡

這樣問題就會從「估計真實 3D 堆高」變成「讀取一個半結構化版面」。

## 照片 => 計算碼量的 MVP 解法

### 核心思路

不要直接算「每一疊有幾片」，而是先把問題拆成三個簡單步驟：

1. 找到有幾個顏色群組
2. 找到每個顏色群組中有幾疊
3. 讓使用者只補最少資訊

### 最推薦的 MVP 版本

使用者流程：

1. 玩家先建立一組 chip set
2. 設定每個顏色代表的面額
3. 拍攝自己的籌碼區
4. 系統先辨識畫面中有哪些顏色群組與幾疊
5. 使用者快速確認每個顏色的：
   - 面額
   - 每疊固定幾顆
   - 是否有零頭
6. 系統算出總碼量與 BB

計算公式：

- 某顏色總顆數 = 疊數 x 每疊顆數 + 零頭顆數
- 某顏色總碼量 = 某顏色總顆數 x 該顏色面額
- 總碼量 = 所有顏色總碼量加總
- BB = 總碼量 / 大盲

### 這個版本的關鍵假設

- 系統不一定要知道每一疊的真實高度
- 只要玩家遵守「同色一疊代表固定顆數」即可
- 使用者手動補的資訊要非常少

例如：

- 紅色：3 疊，每疊 20
- 綠色：2 疊，每疊 20，外加零頭 7
- 黑色：1 疊，每疊 10

這比讓系統自己估每一疊有幾片容易太多。

## 手動輸入怎麼設計才會簡單

重點不是消滅手動，而是把手動壓縮成 5 到 10 秒內能完成。

### 方案 A：先設定 chip set，之後只修數量

第一次建立牌局時先做一次設定：

- 紅色 = 100
- 綠色 = 500
- 黑色 = 1000
- 白色 = 25

之後每次拍照時不需要再輸入顏色代表多少碼，只需要修正：

- 偵測到幾疊
- 每疊顆數是否為 20
- 是否有零頭

這是最推薦的方式。

### 方案 B：每個顏色做大按鈕快速修正

拍照後直接出現卡片：

- 紅色：偵測 3 疊，面額 100
- 綠色：偵測 2 疊，面額 500
- 黑色：偵測 1 疊，面額 1000

每張卡片只給幾個超快操作：

- `+1 疊`
- `-1 疊`
- `零頭`
- `面額`

這樣使用者不需要打字，多數情況只要點幾下。

### 方案 C：用模板化輸入而不是自由輸入

不要讓使用者每次都輸入完整數字，而是提供模板：

- 每疊 20 顆
- 每疊 10 顆
- 自訂

零頭也提供快捷：

- 0
- 1 到 5
- 10
- 自訂

### 方案 D：支援兩種精度模式

粗估模式：

- 只記錄總碼量或 BB
- 適合牌局中快速更新

精算模式：

- 記錄每種顏色的疊數與零頭
- 適合休息時間或局後整理

這樣就不會逼使用者每次都走完整流程。

## 比 YOLO 更值得優先做的辨識邏輯

MVP 階段不一定要先訓練偵測模型，可以先用更便宜的方法：

### 1. 顏色分群

如果拍攝條件有控制好，先用顏色區分往往比 YOLO 更實際。

可行方式：

- 先把影像轉 HSV
- 對已知 chip color 做 threshold
- 找出每種顏色的主要區塊
- 估計每種顏色有幾個頂面或幾個群組

前提是：

- 只支援少數固定顏色
- 光線不要太亂
- 使用者先在設定頁定義 chip set

### 2. 固定拍攝區 + 版面引導

如果 UI 上提示使用者把籌碼放進幾個區塊，例如：

- 白色區
- 紅色區
- 綠色區
- 黑色區

那演算法甚至不用先猜顏色，只要在各區內數疊數即可。

這比通用 YOLO 容易非常多。

### 3. 影片只拿來選最佳照片

如果未來要支援影片，建議先做：

- 錄 3 秒
- 從中挑出最清楚、最穩的一張
- 後續仍然走單張圖片分析

不要一開始做完整 video tracking。

## 建議的 MVP 拍照流程

### 流程 1：最快可用版

1. 玩家建立牌局
2. 設定大盲與 chip set
3. 玩家按下更新碼量
4. 玩家拍一張自己籌碼的俯視照片
5. 系統偵測顏色群組與可能疊數
6. 使用者點幾下修正
7. 系統顯示總碼量與 BB

### 流程 2：更穩的輔助版

1. 玩家把同色籌碼分開堆放
2. 每一疊固定代表 20 顆
3. 零頭平鋪
4. 系統辨識每種顏色有幾疊
5. 使用者只輸入零頭
6. 系統自動算總碼量

這個流程是目前最務實的方案。

## MVP 功能清單

### 一定要做

- 建立牌局
- 設定 SB / BB
- 設定 chip colors 與面額
- 記錄 buy-in / rebuy / add-on
- 顯示目前碼量
- 顯示目前 BB
- 拍照後人工修正疊數

### 應該做

- 儲存最近一次碼量快照
- 顯示碼量變化
- 支援粗估模式與精算模式
- 支援照片結果手動覆寫

### 先不要做

- 全自動算堆高
- 整桌多人同時辨識
- 連續影片追蹤
- 自動記錄每手 action

## 推薦的產品版本切法

### V0

純手動：

- 設定盲注
- 設定 chip set
- 手動輸入目前總碼量
- 自動換算 BB

目的是先驗證使用者是否真的需要桌邊記錄。

### V1

照片輔助：

- 拍照
- 偵測顏色群組
- 顯示每種顏色的疊數估計
- 使用者修正
- 計算總碼量與 BB

### V2

短影片輔助：

- 錄 3 到 5 秒
- 自動挑最佳 frame
- 用同一套照片流程分析

## 現階段技術建議

基於目前 repo 狀況，最適合先做的是：

1. 先做產品流程與資料模型
2. 不把核心壓在 YOLO 訓練
3. 先做半手動的照片計算
4. 先支援少數固定顏色
5. 先要求使用者配合擺放規則

## 一句話結論

最實際的 MVP 不是「AI 自己算出所有籌碼」，而是「AI 幫忙把使用者的操作減到最少，最後仍由使用者快速確認」。
